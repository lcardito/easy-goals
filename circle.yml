deployment:
  production:
    branch: master
    commands:
      - |
        cat >~/.netrc <<EOF
        machine api.heroku.com
          login lcardito@gmail.com
          password lca81ce4477-ef1c-4a43-8b7a-d2aaaf234ddf
        machine git.heroku.com
          login lcardito@gmail.com
          password $81ce4477-ef1c-4a43-8b7a-d2aaaf234ddf
        EOF
      - chmod 600 ~/.netrc
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
      - git push git@heroku.com:simple-goals.git $CIRCLE_SHA1:refs/heads/master