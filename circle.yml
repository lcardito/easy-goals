deployment:
  staging:
    branch: master
    heroku:
      appname: simple-goals
dependencies:
  pre:
    - mkdir server/node_modules
    - mkdir client/node_modules
  cache_directories:
    - "server/node_modules"
    - "client/node_modules"